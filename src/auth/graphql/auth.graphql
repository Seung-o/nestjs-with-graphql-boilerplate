type Mutation {
  socialSignup(input: SocialSignupInput!): User!
}

input SocialSignupInput {
  email: String!
  name: String!
  provider: UserProvider!
}

extend type User {
  accessToken: String!
}
